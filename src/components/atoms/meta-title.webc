<script webc:type="render" webc:is="template">
	function title() {
		let title;

		if (this.$data.title) {
			title = `${this.$data.title} | ${this.$data.site.name}`;
		} else {
			title = this.$data.site.name;
		}

		return `<title>${title}</title>`;
	}
</script>
