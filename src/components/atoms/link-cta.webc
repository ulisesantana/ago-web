<a :href="this.href" class="link-cta" :alt="this.cta">
  <button @text="this.cta"></button>
</a>

<style webc:scoped>
.link-cta {
  display: block;
  margin: 2rem auto;
  width: fit-content;
}

button {
  background-color: var(--primary-color);
  border: none;
  border-radius: var(--border-radius);
  color: var(--bg-color);
  font-size: 1.25rem;
  padding: 0.5rem;
}
</style>